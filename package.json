{
  "name": "jcode-ext-mermaid-prev",
  "version": "0.1.0",
  "private": true,
  "description": "Mermaid Preview extension for JCode — built with esbuild + TypeScript.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "marked": "^14.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.1",
    "typescript": "^5.5.4"
  }
}
